@model IReadOnlyList<BreadCrumbsItem>

<nav class="breadcrumbs">
	<ul>
		@if (Model.Count > 1)
		{
			@for (var i = 0; i < Model.Count - 1; i++)
			{
				<li><a asp-culture-page="@Model[i].Route" htmx-menu-item>@Model[i].Name</a></li>
			}
		}
	</ul>
</nav>
