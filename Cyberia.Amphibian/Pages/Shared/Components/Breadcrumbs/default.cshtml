@model IReadOnlyList<BreadCrumbsItem>

<nav class="breadcrumbs">
	<ul>
		@if (Model.Count > 1)
		{
			@for (var i = 0; i < Model.Count - 1; i++)
			{
				<li><a asp-route-culture="@ViewBag.Culture" asp-page="@Model[i].Route">@Model[i].Name</a></li>
			}
		}
	</ul>
</nav>
